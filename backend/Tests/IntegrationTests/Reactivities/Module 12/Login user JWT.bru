meta {
  name: Login user JWT
  type: http
  seq: 1
}

post {
  url: {{url}}/api/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
  	"email": "bob@test.com",
  	"password": "Pa$$w0rd"
  }
}

settings {
  encodeUrl: true
}
